写一个程序，作用就是在 windows 界面显示一个很复杂的图片（比如超级复杂的二维码），并不断刷新，可以控制刷新率。
作用是测试moonlight + sunshine 串流时，不同码率+帧率情况下的编解码延迟，网络延迟。

1. 使用 python 编写
2. 二维码刚开始使用标准的二维码库。后面自己实现一个没有意义的二维码，有颜色+每个像素点有形状区别，用于增加单帧二维码的复杂度。不需要能够传输数据。
3. 可以控制二维码大小，来控制码率
4. 可以控制刷新帧率
5. 刷新要有足够性能，能够支持超高刷新率，比如 165Hz, 365Hz
6. 目前性能太差了，只实现了 40 fps 的刷新率，做到 python 实现的极限
7. 我发现固定的搭配能够让码率最高，比如 imagesize=711, grid cell=144, target fps=165, frame pool=5。imagesize 加1 减1 都会让码率骤然下
  降。目前能够达到的码率是 640Mbps，已经够用了。请将上述参数打包成默认参数。把位于上面的配置选项放到右侧 垂直排列。并且滑动条右侧数字
  可以直接编辑。增加文字说明每个选项含义。 后续：有发现需要反复 rebuild frames 抽奖，才可以抽到最高的 640Mbps 码率